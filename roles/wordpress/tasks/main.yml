---
- mysql_db:
    name: "{{ db_name }}"

- mysql_user:
    name: "{{ db_name }}"
    password: "{{ _mysql.passwords[db_name] }}"
    priv: "{{ db_name }}.*:ALL"
    host: "{{ php_ip }}"

- lineinfile: dest=/etc/mysql/backup.txt line={{ db_name }}

- file:
    path: "{{ caddy_root }}/{{ item }}"
    state: directory
    mode: "a+rX"
    owner: www-data
    group: www-data
    recurse: yes
  with_items:
    - wordpress
    - logs

- copy: src=php.ini dest={{ caddy_root }}/php.ini
  notify: restart {{ name }}

- template:
    src: Caddyfile.j2
    dest: "{{ caddy_root }}/Caddyfile"
    validate: /usr/local/bin/caddy-from-source -conf %s -validate
  notify: reload caddy

- file:
    src: "{{ caddy_root }}/Caddyfile"
    dest: /etc/caddy/vhosts/{{ name }}.conf
    state: link

- template: dest=/etc/systemd/system/wordpress-{{ name }}.service src=wordpress.service.j2
  notify:
    - reload systemd
    - restart {{ name }}

- meta: flush_handlers

- service: name=wordpress-{{ name }} state=started enabled=yes
